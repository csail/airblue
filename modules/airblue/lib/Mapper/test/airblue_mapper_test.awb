********************************************************************
* Awb module specification
********************************************************************

%AWB_START

%name Airblue Mapper Test
%desc Airblue Mapper Test
%provides hw_only_application

%requires airblue_common
%requires airblue_types
%requires airblue_mapper

%attributes airblue

%sources -t BSV -v PUBLIC MapperTest.bsv

%generated -t BA viterbiMapCtrl.ba

%AWB_END
